jwt:
  secret:
    key: ${JWT_SECRET}

spring:
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS-KEY}
        secret-key: ${AWS_PRIVATE-KEY}
      s3:
        bucket: ${AWS_S3_BUCKET}
        region: ${AWS_S3_REGION}